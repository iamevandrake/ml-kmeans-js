# K-Means Machine Learning Algorithm

This is a k-means algorithm with sample data written in javascript.

It works with 2d sample data in the data.js file.

Run this algorithm with yarn start to see the k-means.
